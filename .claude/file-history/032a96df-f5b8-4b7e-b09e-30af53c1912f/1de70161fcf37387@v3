# SinglePTZ-FaceTrack 详细设计书

**版本**：v0.2（含用户反馈修订）
**文档类型**：SDD (Software Design Document)
**对应需求**：`doc/需求说明书.md` v0.2

---

## 1. 设计目标

在《需求说明书》框定的范围内，给出一份**可直接动手实现**的 Python 工程方案：

- 模块边界清晰、单进程运行；
- **3 预设位巡航 + 顺序扫描**的完整状态机；
- 适配 **2 倍光学变焦**的约束（主要靠 PTZ 转向 + 4MP 分辨率，而非大倍率光学放大）；
- SDK/模型/业务可独立替换；
- 所有配置走 YAML。

---

## 2. 总体架构

```
                    ┌───────────────────────────────────────────┐
                    │           ScanScheduler 状态机             │
                    │  PATROL ↔ SCAN_PRESET[1,2,3] ↔ ZOOM_SCAN │
                    └───┬──────────────┬──────────────┬─────────┘
                        │              │              │
              ┌─────────▼───────┐ ┌────▼────────┐ ┌──▼───────────┐
              │  VideoSource    │ │ DetectorHub │ │ HikPTZ       │
              │ (RTSP→frames)  │ │ face1280    │ │ (SDK ctypes)  │
              │ 后台线程最新帧  │ │ face640     │ │ login/preset  │
              └─────────────────┘ │ person      │ │ 3D定位/录像   │
                                  └──────┬──────┘ └──────────────┘
                                         │
                        ┌────────────────┼────────────────┐
                        │                │                │
                 ┌──────▼──────┐  ┌──────▼──────┐  ┌─────▼────────┐
                 │ Tracker     │  │ ArcFace     │  │ OSNet ReID   │
                 │ SORT+ReID  │  │ 512-d embed │  │ 512-d embed  │
                 └──────┬──────┘  └──────┬──────┘  └──────────────┘
                        │                │
                 ┌──────▼────────────────▼──────┐
                 │    FaceGallery (photo/→emb)  │
                 └──────┬───────────────────────┘
                        │
                 ┌──────▼───────────────────────┐
                 │ Visualizer + EventLogger     │
                 │ + SnapshotSaver + Recorder   │
                 └──────────────────────────────┘
```

**线程模型**：单主线程 + 1 个后台取流线程。主线程驱动状态机，后台线程只负责 `cap.grab() / retrieve()` 缓存最新帧。

---

## 3. 目录结构

```
SinglePTZ-FaceTrack/
├── HCNetSDKV6.../             # 保持原状
├── doc/
│   ├── 需求说明书.md
│   ├── 详细设计书.md          # ← 本文
│   └── 枪击人脸放大变zoom操作说明.md
├── models/
│   ├── facedect/{640,1280}/   # 保持原状
│   ├── facerecognize/         # 保持原状
│   ├── person/                # ← 新增
│   │   └── yolov8n.onnx
│   └── reid/                  # ← 新增
│       └── osnet_x0_25.onnx
├── photo/                     # 保持原状（人脸库源图）
├── cache/
│   └── gallery.npz            # 人脸库 embedding 缓存
├── config/
│   └── config.yaml            # 所有参数
├── logs/
│   └── app.log
├── output/
│   ├── events.jsonl
│   └── strangers/
├── scripts/
│   └── gallery_health.py      # 人脸库自测脚本
└── src/
    ├── __init__.py
    ├── main.py                 # 程序入口
    ├── config.py               # YAML → dataclass
    ├── sdk/
    │   ├── hik_sdk.py          # ctypes 绑定 + 结构体定义
    │   └── hik_ptz.py          # PTZ 业务封装（login/preset/3D/录像）
    ├── video/
    │   └── rtsp_source.py      # 后台线程帧缓存
    ├── detect/
    │   ├── yolo_face.py        # 640 & 1280 共用
    │   └── yolo_person.py      # YOLOv8n person 过滤
    ├── reid/
    │   └── osnet.py            # OSNet ReID embedding
    ├── track/
    │   └── sort_reid.py        # SORT + ReID 身份维持
    ├── recognize/
    │   ├── arcface.py          # ArcFace embedder
    │   └── gallery.py          # photo/ → 索引 → 缓存
    ├── scheduler/
    │   └── state_machine.py    # 核心：巡航 + 扫描调度
    ├── ui/
    │   └── visualizer.py       # OpenCV 叠加 + 键盘交互
    └── utils/
        ├── logger.py
        ├── quality.py          # 清晰度/角度/尺寸过滤
        └── geometry.py         # bbox 外扩/归一化/映射
```

---

## 4. 核心数据结构

```python
from dataclasses import dataclass, field
from enum import IntEnum
from typing import Optional, Tuple, List, Dict
import numpy as np

# ─── 检测 ───
@dataclass
class Detection:
    bbox: Tuple[int, int, int, int]     # x1,y1,x2,y2 (原图坐标)
    score: float
    cls_id: int = 0                     # 0=face, 1=person

# ─── 跟踪 ───
class ScanStatus(IntEnum):
    PENDING         = 0   # 待扫描
    SCANNING        = 1   # 正在放大扫描中
    DONE_HIT        = 2   # 识别命中库内
    DONE_STRANGER   = 3   # 确认陌生人
    DONE_UNRECOGNIZED = 4 # 质量不够 / 超时
    LOST            = 5   # 扫描中途丢失

@dataclass
class Track:
    track_id: int
    bbox: Tuple[int, int, int, int]
    kalman_state: np.ndarray
    face_feat: Optional[np.ndarray] = None     # ArcFace 512-d（识别用）
    reid_feat: Optional[np.ndarray] = None     # OSNet  512-d（ReID 用）
    last_seen_ts: float = 0.0
    scan_status: ScanStatus = ScanStatus.PENDING
    scan_result: Optional[str] = None
    scan_sim: float = 0.0
    preset_id: int = 0                         # 发现该 track 的预设位
    age: int = 0
    hits: int = 0

# ─── 人脸库 ───
@dataclass
class GalleryEntry:
    name: str
    feat: np.ndarray          # (512,) L2 归一化
    src_files: List[str] = field(default_factory=list)

# ─── 识别结果 ───
@dataclass
class MatchResult:
    kind: str                 # "hit" / "stranger" / "ambiguous"
    name: Optional[str]
    sim: float

# ─── 全局已识别表（跨预设位去重用）───
@dataclass
class IdentifiedPerson:
    track_id: int
    reid_feat: np.ndarray     # OSNet
    scan_result: str          # 姓名 or "STRANGER"
    scan_sim: float
    preset_id: int            # 首次识别的预设位
```

---

## 5. 关键模块设计

### 5.1 海康 SDK 绑定 `src/sdk/hik_sdk.py`

用 `ctypes` 加载 `libhcnetsdk.so`，声明必要的结构体和函数签名。

```python
import ctypes as C
from ctypes import c_int, c_long, c_uint, c_char, c_byte, POINTER, Structure, byref

class NET_DVR_USER_LOGIN_INFO(Structure):
    _fields_ = [
        ("sDeviceAddress", c_char * 129),
        ("byUseTransport", c_byte),
        ("wPort", C.c_ushort),
        ("sUserName", c_char * 64),
        ("sPassword", c_char * 64),
        ("cbLoginResult", C.c_void_p),
        ("pUser", C.c_void_p),
        ("bUseAsynLogin", c_int),
        ("byRes2", c_byte * 128),
    ]

class NET_DVR_DEVICEINFO_V40(Structure): ...

class NET_DVR_POINT_FRAME(Structure):
    _fields_ = [
        ("xTop", c_int), ("yTop", c_int),
        ("xBottom", c_int), ("yBottom", c_int),
        ("bCounter", c_int),
    ]

# 函数签名
# NET_DVR_Init / Cleanup
# NET_DVR_Login_V40 / Logout
# NET_DVR_PTZSelZoomIn_EX(lUserID, lChannel, pPointFrame) → BOOL
# NET_DVR_PTZPreset_Other(lUserID, lChannel, dwCmd, dwPresetIndex) → BOOL
#   dwCmd: SET_PRESET=8, CLE_PRESET=9, GOTO_PRESET=39
# NET_DVR_PTZCruise_Other(lUserID, lChannel, dwCmd, byCruiseRoute, byCruisePoint, wInput) → BOOL
#   dwCmd: FILL_PRE_SEQ=30, SET_SEQ_DWELL=31, SET_SEQ_SPEED=32,
#          CLE_PRE_SEQ=33, RUN_SEQ=37, STOP_SEQ=38
# NET_DVR_PTZControl_Other(lUserID, lChannel, dwCmd, dwStop) → BOOL
#   dwCmd: ZOOM_IN=11, ZOOM_OUT=12, FOCUS_NEAR=13, FOCUS_FAR=14
# NET_DVR_StartDVRRecord(lUserID, lChannel, lRecordType) → BOOL
# NET_DVR_StopDVRRecord(lUserID, lChannel) → BOOL
# NET_DVR_GetDeviceAbility(lUserID, dwAbilityType, pInBuf, dwInLen, pOutBuf, dwOutLen) → BOOL
# NET_DVR_GetLastError() → DWORD
```

> **LD_LIBRARY_PATH 注意**：`libhcnetsdk.so` 内部 `dlopen` 加载其他 .so。必须在 Python 进程启动前（如启动脚本或 `.bashrc`）设好 `LD_LIBRARY_PATH` 指向 SDK 库文件目录，**不能**在 `CDLL` 之后再改。

### 5.2 PTZ 业务封装 `src/sdk/hik_ptz.py`

```python
class HikPTZ:
    GOTO_PRESET = 39

    def __init__(self, cfg): ...
    def login(self) -> None: ...
    def logout(self) -> None: ...

    def check_3d_positioning(self) -> bool:
        """GetDeviceAbility(DEVICE_ABILITY_INFO) → XML 查 <PTZZoomIn>"""

    def goto_preset(self, preset_id: int) -> bool:
        """NET_DVR_PTZPreset_Other(user_id, channel, GOTO_PRESET=39, preset_id)"""

    def zoom_to_bbox(self, bbox, frame_w, frame_h, expand=1.5) -> bool:
        """bbox → 外扩 → 归一化 [0,255] → NET_DVR_PTZSelZoomIn_EX
        公式：
          cx, cy = center of bbox
          w, h   = bbox size * expand
          x1,y1  = max(cx-w/2, 0), max(cy-h/2, 0)
          x2,y2  = min(cx+w/2, frame_w), min(cy+h/2, frame_h)
          pf.xTop    = int(x1 * 255 / frame_w)
          pf.yTop    = int(y1 * 255 / frame_h)
          pf.xBottom = int(x2 * 255 / frame_w)
          pf.yBottom = int(y2 * 255 / frame_h)
        """

    def start_record(self) -> bool:
        """NET_DVR_StartDVRRecord(user_id, channel, 0)  # 0=手动"""

    def stop_record(self) -> bool:
        """NET_DVR_StopDVRRecord(user_id, channel)"""
```

### 5.3 视频取流 `src/video/rtsp_source.py`

```python
class RtspSource:
    """后台线程 grab，主线程 retrieve 最新帧。丢老帧保新帧。"""
    def __init__(self, url: str, reconnect_interval=2.0, max_retries=12): ...
    def start(self) -> None: ...
    def read(self) -> Optional[np.ndarray]: ...  # 返回最新帧或 None
    def stop(self) -> None: ...

    def _loop(self):
        retries = 0
        while not self._stop:
            cap = cv2.VideoCapture(self.url, cv2.CAP_FFMPEG)
            cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)
            if not cap.isOpened():
                retries += 1
                if retries > self.max_retries: break
                time.sleep(self.reconnect_interval)
                continue
            retries = 0
            while not self._stop:
                ok = cap.grab()
                if not ok: break
                ok, frame = cap.retrieve()
                if ok:
                    with self._lock:
                        self._latest = frame
                        self._ts = time.time()
            cap.release()
            time.sleep(self.reconnect_interval)
```

### 5.4 人脸检测 `src/detect/yolo_face.py`

输出 `(1, 5, N)` 未做 NMS，5 = `cx, cy, w, h, conf`，单类 face。

```python
class YoloFace:
    def __init__(self, onnx_path, input_size, conf=0.35, iou=0.5, providers=None): ...
    def detect(self, img_bgr) -> List[Detection]:
        # 1. letterbox（保持长宽比）到 input_size × input_size
        # 2. 推理 → (1,5,N)
        # 3. 转置 → (N,5) → 置信度过滤
        # 4. cxcywh → xyxy
        # 5. NMS (cv2.dnn.NMSBoxes)
        # 6. 反 letterbox 映射回原图坐标
```

**两个实例**：全景态 `YoloFace(1280)`，近景态 `YoloFace(640)`。

### 5.5 人体检测 `src/detect/yolo_person.py`

YOLOv8n COCO，只保留 `class_id == 0`（person）。

```python
class YoloPerson:
    def __init__(self, onnx_path, conf=0.3, iou=0.5, providers=None): ...
    def detect(self, img_bgr) -> List[Detection]:
        # 与 YoloFace 流程一致，但 cls 过滤只留 person
```

### 5.6 人脸识别 `src/recognize/arcface.py`

```python
class ArcFace:
    def embed(self, face_bgr) -> np.ndarray:
        """任意尺寸 face crop → resize 112×112 → RGB → 归一化 → 推理 → L2 单位化"""
        img = cv2.resize(face_bgr, (112, 112))
        img = img[:,:,::-1].astype(np.float32)
        img = (img - 127.5) / 128.0
        img = img.transpose(2,0,1)[None]
        feat = self.sess.run(None, {self.input_name: img})[0][0]
        return feat / (np.linalg.norm(feat) + 1e-9)
```

> **对齐**：当前无 landmark，bbox 外扩 1.3 倍直接 resize。后续可加 PFLD。

### 5.7 人脸库 `src/recognize/gallery.py`

```python
class FaceGallery:
    entries: Dict[str, GalleryEntry]

    def build_or_load(self): ...
    def match(self, feat, match_th=0.35, reject_th=0.20) -> MatchResult:
        sims = gallery_matrix @ feat   # (K,512) @ (512,) → (K,)
        top = argmax(sims)
        if sims[top] >= match_th:  return MatchResult("hit", ...)
        if sims[top] <  reject_th: return MatchResult("stranger", ...)
        return MatchResult("ambiguous", ...)
```

### 5.8 ReID `src/reid/osnet.py`

OSNet_x0_25，ONNX，输入 `1×3×256×128`，输出 512-d。

```python
class OSNetReID:
    def embed(self, person_bgr) -> np.ndarray:
        img = cv2.resize(person_bgr, (128, 256))
        img = img[:,:,::-1].astype(np.float32) / 255.0
        img = (img - [0.485,0.456,0.406]) / [0.229,0.224,0.225]
        img = img.transpose(2,0,1)[None]
        feat = self.sess.run(None, {self.input_name: img})[0][0]
        return feat / (np.linalg.norm(feat) + 1e-9)
```

### 5.9 Tracker `src/track/sort_reid.py`

SORT 框架 + ReID 代价混合：

```
cost[i,j] = α * (1 - IoU(det_i, track_j)) + β * (1 - cosine(reid_det_i, reid_track_j))
  α=0.6, β=0.4
```

**关键**：
- Track 的 `reid_feat` EMA 更新（权重 0.1）；
- 跨"预设位→zoom→回预设位"时，用 snapshot + ReID 匈牙利匹配复用 track_id；
- 跨预设位去重通过**全局已识别表**（`IdentifiedPerson` 列表）。

### 5.10 调度器状态机 `src/scheduler/state_machine.py`

**这是本系统最核心的模块。**

#### 5.10.1 状态定义

```python
class State(IntEnum):
    INIT             = 0   # 启动初始化
    PATROL_GOTO      = 1   # 巡航：正在移动到下一个预设位
    PATROL_DWELL     = 2   # 巡航：在某预设位停留检测中
    SCAN_GOTO_PRESET = 3   # 扫描：正在移动到目标预设位
    SCAN_DETECT      = 4   # 扫描：在预设位做人脸检测 + 建队列
    SCAN_PICK        = 5   # 扫描：从队列取下一个目标
    SCAN_ZOOM_IN     = 6   # 扫描：3D 定位放大
    SCAN_SETTLE      = 7   # 扫描：等稳图
    SCAN_CAPTURE     = 8   # 扫描：近景采集 + 质检
    SCAN_RECOGNIZE   = 9   # 扫描：ArcFace 匹配
    SCAN_ZOOM_OUT    = 10  # 扫描：回到当前预设位
    SCAN_NEXT_PRESET = 11  # 扫描：当前预设位扫完，切下一个
```

#### 5.10.2 状态转换图

```
                          ┌─────────────────────────────────────┐
                          │                                     │
    ┌──────┐   ┌──────────▼──────────┐   ┌─────────────────┐   │
    │ INIT ├──►│ PATROL_GOTO         ├──►│ PATROL_DWELL    │───┘
    └──────┘   │ (goto preset[idx])  │   │ (检测 N 秒)     │
               └─────────────────────┘   └───────┬─────────┘
                         ▲                       │ 发现人脸！
                         │                       ▼
               ┌─────────┴───────┐   ┌──────────────────────┐
               │ 3 个都扫完       │   │ SCAN_GOTO_PRESET     │◄──────┐
               │ 回到 PATROL     │   │ (goto 目标预设位)      │       │
               └─────────────────┘   └──────────┬───────────┘       │
                                                ▼                    │
                                     ┌──────────────────────┐       │
                                     │ SCAN_DETECT          │       │
                                     │ (1280检测+person+ReID│       │
                                     │  建扫描队列)         │       │
                                     └──────────┬───────────┘       │
                                                ▼                    │
                                     ┌──────────────────────┐       │
                                     │ SCAN_PICK            │       │
                                     │ 队列取下一个 PENDING  │───┐   │
                                     └──────────┬───────────┘   │   │
                                                │ 有目标        │   │
                                                ▼               │   │
                                     ┌──────────────────────┐   │   │
                                     │ SCAN_ZOOM_IN         │   │   │
                                     │ PTZSelZoomIn_EX      │   │   │
                                     └──────────┬───────────┘   │   │
                                                ▼               │   │
                                     ┌──────────────────────┐   │   │
                                     │ SCAN_SETTLE (帧差)   │   │   │
                                     └──────────┬───────────┘   │   │
                                                ▼               │   │
                                     ┌──────────────────────┐   │   │
                                     │ SCAN_CAPTURE (640)   │   │   │
                                     └──────────┬───────────┘   │   │
                                                ▼               │   │
                                     ┌──────────────────────┐   │   │
                                     │ SCAN_RECOGNIZE       │   │   │
                                     │ ArcFace+gallery      │   │   │
                                     └──────────┬───────────┘   │   │
                                                ▼               │   │
                                     ┌──────────────────────┐   │   │
                                     │ SCAN_ZOOM_OUT        │   │   │
                                     │ 回当前预设位          │───┘   │
                                     └──────────┬───────────┘       │
                                                │ 队列空            │
                                                ▼                    │
                                     ┌──────────────────────┐       │
                                     │ SCAN_NEXT_PRESET     ├───────┘
                                     │ idx++ (还有预设位)     │
                                     └──────────┬───────────┘
                                                │ 3 个都扫完
                                                ▼
                                     回到 PATROL_GOTO
```

#### 5.10.3 状态转换表

| 当前状态 | 触发条件 | 下一状态 | 动作 |
|---|---|---|---|
| INIT | 初始化完成 | PATROL_GOTO | login, build gallery, start rtsp |
| PATROL_GOTO | `goto_preset()` 已调用 | PATROL_DWELL | 设 settle 超时 |
| PATROL_DWELL | 帧差稳定 & 无人脸 & 停留时间到 | PATROL_GOTO | `patrol_idx = (patrol_idx+1) % 3` |
| PATROL_DWELL | 帧差稳定 & **检测到人脸** | SCAN_GOTO_PRESET | 记录触发预设位; `scan_preset_queue = [当前, 下一个, 再下一个]` |
| SCAN_GOTO_PRESET | `goto_preset()` 已调用 | SCAN_DETECT | 设 settle 超时 |
| SCAN_DETECT | 稳图后执行一次检测 | SCAN_PICK | 建立扫描队列（排除已在全局已识别表中的人） |
| SCAN_PICK | 队列有 PENDING 目标 | SCAN_ZOOM_IN | 取出目标 T，mark SCANNING |
| SCAN_PICK | 队列空 | SCAN_NEXT_PRESET | — |
| SCAN_ZOOM_IN | SDK OK | SCAN_SETTLE | 设 settle deadline 1.5s |
| SCAN_ZOOM_IN | SDK 失败 | SCAN_ZOOM_OUT | T = DONE_UNRECOGNIZED |
| SCAN_SETTLE | 帧差 < τ 或超时 | SCAN_CAPTURE | 初始化 capture_buf |
| SCAN_CAPTURE | 采到 ≥3 张 或超时 3s | SCAN_RECOGNIZE | — |
| SCAN_CAPTURE | 超时且 0 张 | SCAN_ZOOM_OUT | T = DONE_UNRECOGNIZED |
| SCAN_RECOGNIZE | hit / stranger | SCAN_ZOOM_OUT | 更新 T + 全局已识别表 + event log |
| SCAN_RECOGNIZE | ambiguous & 未超时 | SCAN_CAPTURE | 继续采 |
| SCAN_RECOGNIZE | ambiguous & 超时 | SCAN_ZOOM_OUT | 按 top1 兜底 |
| SCAN_ZOOM_OUT | `goto_preset(当前预设位)` + 稳图 | SCAN_PICK | 回到预设位继续下一个人 |
| SCAN_NEXT_PRESET | 还有未扫的预设位 | SCAN_GOTO_PRESET | `scan_preset_idx++` |
| SCAN_NEXT_PRESET | 3 个都扫完 | PATROL_GOTO | 清空本轮状态，重进巡航 |

#### 5.10.4 巡航策略

```python
# 巡航预设位顺序
patrol_presets = [1, 2, 3]   # config 里可配
patrol_idx = 0               # 当前巡航到哪个

# 每个预设位停留 patrol_dwell 秒，期间持续帧检测
# 若任一帧检测到人脸(且 face count > 0 持续 min_confirm_frames=2 帧) → 中断巡航
```

#### 5.10.5 扫描策略

```python
# 触发扫描后，生成要扫的预设位列表：
# 从触发预设位开始，按顺序扫完 3 个
# 例如在 Preset#2 发现人 → scan_order = [2, 3, 1]
scan_preset_queue = deque()
trigger_idx = patrol_presets.index(trigger_preset)
for i in range(len(patrol_presets)):
    scan_preset_queue.append(patrol_presets[(trigger_idx + i) % len(patrol_presets)])
```

#### 5.10.6 跨预设位去重

```python
# 全局已识别表
identified: List[IdentifiedPerson] = []

def is_already_identified(reid_feat: np.ndarray, threshold=0.5) -> Optional[IdentifiedPerson]:
    """检查一个 person 的 ReID 特征是否已在全局表里"""
    for p in identified:
        sim = np.dot(reid_feat, p.reid_feat)
        if sim > threshold:
            return p
    return None

# 在 SCAN_DETECT 阶段：
# 对每个检测到的 person，提取 OSNet 特征
# 调用 is_already_identified()，若命中则直接标 DONE（复用结果），不入扫描队列
```

#### 5.10.7 主循环伪代码

```python
def run(self):
    self.state = State.INIT
    target: Optional[Track] = None
    patrol_idx = 0
    scan_preset_queue = deque()
    current_scan_preset = None
    identified: List[IdentifiedPerson] = []

    while not self.stop_flag:
        frame = self.video.read()
        if frame is None:
            continue

        if self.state == State.INIT:
            self._init_all()
            self.state = State.PATROL_GOTO

        elif self.state == State.PATROL_GOTO:
            self.ptz.goto_preset(self.cfg.patrol.presets[patrol_idx])
            self.settle_deadline = time.time() + self.cfg.ptz.settle_timeout
            self.dwell_deadline = None
            self.state = State.PATROL_DWELL

        elif self.state == State.PATROL_DWELL:
            if not self._frame_settled(frame) and time.time() < self.settle_deadline:
                self.visualizer.show(frame, state=self.state)
                continue
            if self.dwell_deadline is None:
                self.dwell_deadline = time.time() + self.cfg.patrol.dwell

            # 在停留期间持续检测
            dets = self.face1280.detect(frame)
            self.visualizer.overlay_patrol(frame, dets, self.state, patrol_idx)

            if len(dets) > 0 and self._confirm_faces(dets):
                # 发现人！中断巡航，进入扫描
                trigger = self.cfg.patrol.presets[patrol_idx]
                scan_preset_queue = self._build_scan_order(trigger)
                current_scan_preset = scan_preset_queue.popleft()
                self.state = State.SCAN_GOTO_PRESET
            elif time.time() > self.dwell_deadline:
                # 停留时间到，下一个预设位
                patrol_idx = (patrol_idx + 1) % len(self.cfg.patrol.presets)
                self.state = State.PATROL_GOTO

        elif self.state == State.SCAN_GOTO_PRESET:
            self.ptz.goto_preset(current_scan_preset)
            self.settle_deadline = time.time() + self.cfg.ptz.settle_timeout
            self.state = State.SCAN_DETECT

        elif self.state == State.SCAN_DETECT:
            if not self._frame_settled(frame) and time.time() < self.settle_deadline:
                continue
            # 稳图后做一次完整检测
            face_dets = self.face1280.detect(frame)
            person_dets = self.person_det.detect(frame)
            self.tracker.update(frame, face_dets, person_dets)

            # 跨预设位去重：对每个 PENDING track 的 reid 特征查全局表
            for t in self.tracker.tracks:
                if t.scan_status == ScanStatus.PENDING and t.reid_feat is not None:
                    match = self._is_already_identified(t.reid_feat, identified)
                    if match:
                        t.scan_status = match.scan_status_equiv
                        t.scan_result = match.scan_result
                        t.scan_sim = match.scan_sim
            self.state = State.SCAN_PICK

        elif self.state == State.SCAN_PICK:
            pending = [t for t in self.tracker.tracks
                       if t.scan_status == ScanStatus.PENDING]
            if not pending:
                self.state = State.SCAN_NEXT_PRESET
                continue
            target = self._pick_priority(pending)
            target.scan_status = ScanStatus.SCANNING
            ok = self.ptz.zoom_to_bbox(target.bbox, *frame.shape[1::-1])
            if ok:
                self.settle_deadline = time.time() + self.cfg.ptz.settle_timeout
                self.state = State.SCAN_SETTLE
            else:
                target.scan_status = ScanStatus.DONE_UNRECOGNIZED
                self.state = State.SCAN_ZOOM_OUT

        elif self.state == State.SCAN_SETTLE:
            if self._frame_settled(frame) or time.time() > self.settle_deadline:
                self.capture_buf = []
                self.capture_deadline = time.time() + self.cfg.capture.timeout
                self.state = State.SCAN_CAPTURE

        elif self.state == State.SCAN_CAPTURE:
            dets = self.face640.detect(frame)
            if dets:
                best = self._pick_center_largest(dets, frame.shape)
                face = self._crop_expand(frame, best.bbox, expand=1.3)
                if quality_ok(face):
                    self.capture_buf.append(face)
            if (len(self.capture_buf) >= self.cfg.capture.min_samples
                    or time.time() > self.capture_deadline):
                self.state = State.SCAN_RECOGNIZE

        elif self.state == State.SCAN_RECOGNIZE:
            if not self.capture_buf:
                target.scan_status = ScanStatus.DONE_UNRECOGNIZED
            else:
                feats = [self.arcface.embed(f) for f in self.capture_buf]
                mean_feat = np.mean(feats, axis=0)
                mean_feat /= (np.linalg.norm(mean_feat) + 1e-9)
                res = self.gallery.match(mean_feat,
                          self.cfg.recognize.match_th,
                          self.cfg.recognize.reject_th)
                self._apply_result(target, res, identified)
                self.event_logger.log(target, current_scan_preset)
            self.state = State.SCAN_ZOOM_OUT

        elif self.state == State.SCAN_ZOOM_OUT:
            self.ptz.goto_preset(current_scan_preset)
            self.settle_deadline = time.time() + self.cfg.ptz.settle_timeout
            # 等稳图后回 SCAN_PICK 继续下一个人
            if self._frame_settled(frame) or time.time() > self.settle_deadline:
                self.state = State.SCAN_PICK

        elif self.state == State.SCAN_NEXT_PRESET:
            if scan_preset_queue:
                current_scan_preset = scan_preset_queue.popleft()
                self.tracker.snapshot_before_move()
                self.state = State.SCAN_GOTO_PRESET
            else:
                # 3 个预设位全扫完，回巡航
                identified.clear()
                self.tracker.reset()
                self.state = State.PATROL_GOTO

        # 显示 & 键盘
        self.visualizer.show(frame)
        key = cv2.waitKey(1) & 0xFF
        self._handle_key(key, identified)
```

### 5.11 稳图判定

```python
def _frame_settled(self, frame):
    if self._prev_gray is None:
        self._prev_gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
        return False
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
    diff = cv2.absdiff(gray, self._prev_gray).mean()
    self._prev_gray = gray
    return diff < self.cfg.ptz.settle_diff_th   # 默认 2.0
```

### 5.12 人脸质量检查 `src/utils/quality.py`

```python
def quality_ok(face_bgr, min_w=80, ar_lo=0.6, ar_hi=1.6, blur_th=80.0):
    h, w = face_bgr.shape[:2]
    if w < min_w: return False
    ar = h / (w + 1e-6)
    if ar < ar_lo or ar > ar_hi: return False
    lap = cv2.Laplacian(cv2.cvtColor(face_bgr, cv2.COLOR_BGR2GRAY), cv2.CV_64F).var()
    return lap >= blur_th
```

### 5.13 可视化 `src/ui/visualizer.py`

```python
# 颜色方案
COLOR_HIT       = (0, 255, 0)     # 绿
COLOR_STRANGER  = (0, 0, 255)     # 红
COLOR_PENDING   = (0, 255, 255)   # 黄
COLOR_SCANNING  = (255, 128, 0)   # 橙
COLOR_UNKNOWN   = (180, 180, 180) # 灰

# HUD 左上角：
#   State: PATROL_DWELL | Preset: #2 | Queue: 3 | FPS: 8.2
#   [REC] (红色，录像中才显示)
# 每个 track：bbox + ID + 姓名/STRANGER/状态
# 右侧面板：已识别名单 (scrollable)
```

---

## 6. 配置文件 `config/config.yaml`

```yaml
hik:
  ip: "192.168.0.249"
  port: 8000
  user: "admin"
  password: "vision123456"
  channel: 1
  rtsp_url: "rtsp://admin:vision123456@192.168.0.249:554/Streaming/Channels/101"
  sdk_lib_dir: "HCNetSDKV6.1.11.5_build20251204_linux64_ZH_20260320152102/HCNetSDKV6.1.11.5_build20251204_linux64_ZH/库文件"

patrol:
  presets: [1, 2, 3]        # 3 个预设位编号
  dwell: 4.0                # 每个预设位停留秒数（巡航态）
  min_confirm_frames: 2     # 连续 N 帧检到人脸才确认（防误触发）

models:
  face_wide:  "models/facedect/1280/best.onnx"
  face_close: "models/facedect/640/model.onnx"
  arcface:    "models/facerecognize/model.onnx"
  reid:       "models/reid/osnet_x0_25.onnx"
  person:     "models/person/yolov8n.onnx"

runtime:
  prefer_gpu: true

detect:
  face_wide_conf:  0.30
  face_wide_iou:   0.5
  face_close_conf: 0.35
  face_close_iou:  0.5
  person_conf:     0.3
  person_iou:      0.5

track:
  iou_weight:  0.6
  reid_weight: 0.4
  max_age:     30
  min_hits:    3
  reid_ema:    0.1

ptz:
  expand_ratio:    1.5       # 3D 定位框外扩比例
  settle_diff_th:  2.0       # 帧差均值 < 此值视为稳图
  settle_timeout:  1.5       # 2 倍变焦球机稳图快，1.5 秒超时

capture:
  min_samples: 3
  max_samples: 5
  timeout:     3.0

recognize:
  match_th:  0.35
  reject_th: 0.20

reid:
  cross_preset_th: 0.5      # 跨预设位去重阈值

output:
  strangers_dir: "output/strangers"
  events_jsonl:  "output/events.jsonl"

log:
  level: "INFO"
  file:  "logs/app.log"
```

---

## 7. 数据流

### 7.1 启动序列

```
main.py
  ├─ load config.yaml
  ├─ set LD_LIBRARY_PATH → hik_sdk CDLL
  ├─ HikPTZ.login() → check_3d_positioning()
  ├─ ORT sessions: face1280 / face640 / arcface / osnet / person
  │   (自动选择 CUDAExecutionProvider 或 fallback CPU)
  ├─ FaceGallery.build_or_load() (photo/ → cache/gallery.npz)
  ├─ RtspSource.start()
  └─ ScanScheduler.run()
```

### 7.2 巡航态时序（无人）

```
t=0.00   PATROL_GOTO: goto_preset(1)
t=0.00~1.50  等稳图...
t=1.50   PATROL_DWELL: 开始在 Preset#1 检测
t=1.50~5.50  每帧跑 face1280，均无人脸
t=5.50   停留 4 秒到期 → PATROL_GOTO: goto_preset(2)
t=5.50~7.00  等稳图...
t=7.00   PATROL_DWELL: Preset#2 检测...
...
t=12.0   一圈巡航完成（3 个预设位 × ~4 秒）
```

### 7.3 扫描态时序（多人，单预设位内 2 人）

```
t=0.00   PATROL_DWELL at Preset#2: face1280 检测到 2 张脸！
t=0.05   中断巡航，scan_preset_queue = [2, 3, 1]
t=0.05   SCAN_GOTO_PRESET: goto_preset(2) (已在此位，瞬间稳图)
t=0.10   SCAN_DETECT: 1280 检测 + person 检测 + tracker 建 T1,T2 (PENDING)
t=0.20   跨预设位去重：identified 为空，无匹配
t=0.20   SCAN_PICK: 选 T1 (面积大)
t=0.21   SCAN_ZOOM_IN: PTZSelZoomIn_EX(T1.bbox)
t=0.21~1.50  SCAN_SETTLE (帧差)
t=1.50   SCAN_CAPTURE: face640 采集 3~5 张
t=1.50~3.20  采到 4 张质量合格
t=3.20   SCAN_RECOGNIZE: ArcFace → gallery → hit("张锡宇", 0.47)
t=3.25   写 event, 更新 identified 表
t=3.25   SCAN_ZOOM_OUT: goto_preset(2)
t=3.25~4.50  等稳图
t=4.50   SCAN_PICK: 选 T2
t=4.51   SCAN_ZOOM_IN → SETTLE → CAPTURE → RECOGNIZE → "STRANGER"
t=7.80   SCAN_ZOOM_OUT → SCAN_PICK: 队列空
t=9.00   SCAN_NEXT_PRESET: 下一个 = Preset#3
t=9.00   SCAN_GOTO_PRESET: goto_preset(3) ...
...
t=25.0   3 个预设位全扫完 → PATROL_GOTO
```

### 7.4 跨预设位去重时序

```
Preset#2 扫描完成，identified 中有：
  {track_id=1, reid_feat=v1, result="张锡宇"}
  {track_id=2, reid_feat=v2, result="STRANGER"}

移到 Preset#3，SCAN_DETECT:
  检测到 3 个 person: P_a, P_b, P_c
  P_a.reid_feat 与 v1 余弦=0.72 > 0.5 → 跳过（已识别为张锡宇）
  P_b.reid_feat 与 v2 余弦=0.31 < 0.5 → 新人，入队
  P_c.reid_feat 与 v1 余弦=0.15 < 0.5 → 新人，入队
  本预设位扫描队列 = [P_b, P_c]
```

---

## 8. 异常处理矩阵

| 异常点 | 策略 |
|---|---|
| SDK 登录失败 | 重试 3 次，间隔 2s；仍失败退出并打印错误码 |
| 3D 定位能力集缺失 | 启动断言失败，退出并提示 |
| `PTZSelZoomIn_EX` 返回 FALSE | 该目标 DONE_UNRECOGNIZED，跳下一个 |
| `goto_preset` 返回 FALSE | 打印告警并重试 1 次；仍失败跳过该预设位 |
| RTSP 断流 | 后台线程 5s 内重连；主循环 `read()` 返回 None 跳过 |
| 人脸库某图 detect 不到脸 | warning 跳过，不中断 |
| ORT CUDA 不可用 | fallback CPU |
| 扫描中目标消失 | 3s 超时后 DONE_UNRECOGNIZED |
| 近景多脸 | 选最靠中心、面积最大的一个 |
| 巡航中 PTZ 故障 | 超时后跳到下一个预设位 |
| 录像启动失败 | 打印告警，不影响主流程 |

---

## 9. 性能预算（GPU 基线）

| 环节 | GPU 耗时 | CPU 耗时 |
|---|---|---|
| face1280 推理 | 15~25 ms | 180~250 ms |
| face640 推理 | 8~12 ms | 40~70 ms |
| person (yolov8n) | 8~12 ms | 35~60 ms |
| ArcFace embed ×1 | 3~5 ms | 15~25 ms |
| OSNet embed ×1 | 2~3 ms | 5~10 ms |
| PTZ 3D 定位 + 稳图 | — | 1.0~1.5 s |
| PTZ 预设位切换 + 稳图 | — | 1.0~2.0 s |
| **单人端到端** | — | **2.5~4.0 s** |
| **3 预设位巡航一圈（无人）** | — | **~12 s** |
| **3 预设位 5 人全扫** | — | **~25 s** |

> 瓶颈在 PTZ 机械动作（占单人耗时 60%+），GPU 加速模型推理对总时间改善有限但对帧率有帮助。

---

## 10. 开发里程碑

| Milestone | 产物 | 验收点 |
|---|---|---|
| M1 SDK 打通 | `hik_sdk.py` + `hik_ptz.py` | login / 能力集读取 / goto_preset(1,2,3) / zoom_to_bbox / 录像启停 |
| M2 取流 + 检测 | `rtsp_source.py` + `yolo_face.py` + `yolo_person.py` | OpenCV 窗口画出人脸框 + 人体框 |
| M3 识别 + 库 | `arcface.py` + `gallery.py` + `gallery_health.py` | self-match 正对正 ≥0.6，交叉 <0.2 |
| M4 巡航 MVP | `state_machine.py`（仅 PATROL 态） | 3 个预设位自动循环，检到人脸打日志 |
| M5 单预设位扫描 | 状态机 SCAN 态 | 单预设位 2 人：自动逐个放大→识别→回位 |
| M6 多预设位 + ReID | 完整状态机 + `sort_reid.py` + `osnet.py` | 跨预设位不重扫同一人 |
| M7 可视化 + 日志 | `visualizer.py` + `events.jsonl` | UI + 陌生人抓拍落盘 + 录像控制 |
| M8 调优 & 验收 | 参数微调 | 通过所有 AC-* |

---

## 11. 测试要点

- **单元**：bbox 归一化 & 外扩公式（贴边、超出画面）；YOLO NMS；cosine 匹配。
- **人脸库体检**：`scripts/gallery_health.py` → N×N 相似度矩阵热图，验证同人 >0.6，跨人 <0.3。
- **PTZ 回归**：手工 bbox 验证 3D 定位位置；3 个预设位逐一跳转验证。
- **巡航稳定性**：无人场景下运行 10 分钟，观察巡航是否稳定循环，无误触发。
- **跨预设位去重**：故意让同一人站在两个预设位重叠区域，验证不被重复扫描。
- **离线模拟**：用 `mediamtx` 推一段固定视频到 RTSP，验证状态机在无真机时也可复现。

---

## 12. SDK 关键 API 速查表

| 功能 | 函数 | 参数说明 |
|---|---|---|
| 初始化 | `NET_DVR_Init()` | — |
| 登录 | `NET_DVR_Login_V40(pLoginInfo, pDeviceInfo)` | 返回 lUserID |
| 退出 | `NET_DVR_Logout(lUserID)` | — |
| 能力集 | `NET_DVR_GetDeviceAbility(lUserID, 0x011, ...)` | 0x011 = DEVICE_ABILITY_INFO |
| 预设位 | `NET_DVR_PTZPreset_Other(lUserID, lChannel, cmd, idx)` | cmd: SET=8, CLE=9, GOTO=39 |
| 3D 定位 | `NET_DVR_PTZSelZoomIn_EX(lUserID, lChannel, pPointFrame)` | 坐标 0~255 |
| PTZ 控制 | `NET_DVR_PTZControl_Other(lUserID, lChannel, cmd, stop)` | ZOOM_IN=11, ZOOM_OUT=12 |
| 带速度 PTZ | `NET_DVR_PTZControlWithSpeed_Other(lUserID, lChannel, cmd, stop, speed)` | speed 1~7 |
| 巡航 | `NET_DVR_PTZCruise_Other(lUserID, lChannel, cmd, route, point, input)` | 填充/运行/停止 |
| 录像 | `NET_DVR_StartDVRRecord(lUserID, lChannel, 0)` | 0=手动 |
| 停录 | `NET_DVR_StopDVRRecord(lUserID, lChannel)` | — |
| 错误码 | `NET_DVR_GetLastError()` | — |
| 清理 | `NET_DVR_Cleanup()` | — |
