### Task Report

#### 1. Task Info
- Task ID: M1-T2
- Task Name: Configuration loading module

#### 2. Implementation
- Created `src/config.py` with 13 dataclass definitions covering all config sections
- `load_config(path)` reads YAML and recursively converts to dataclass hierarchy
- Supports nested dataclass conversion and ignores unknown keys

#### 3. Files Modified/Created
- `src/config.py` (new)

#### 4. Test
- Verified all config fields load correctly from config.yaml
- Nested `capture.tracking.safe_zone_ratio` loads properly

#### 5. Status
- Complete: Yes
