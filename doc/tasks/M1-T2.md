### 任务完成报告

#### 1. 任务信息
- 任务ID：M1-T2
- 任务名称：配置加载模块

#### 2. 实现内容
- 实现 `src/config.py`：13 个 dataclass 映射全部配置节
- `load_config(path)` 读取 YAML 并递归转换为 dataclass 层级结构
- 支持嵌套 dataclass 转换，忽略未知键
- 新增 `auto_providers()` 函数，自动检测 CUDA 并回退 CPU

#### 3. 修改/创建文件
- `src/config.py`（新建）

#### 4. 测试情况
- 测试方式：单元测试验证所有配置字段加载
- 测试结果：嵌套字段 `capture.tracking.safe_zone_ratio` 正确加载

#### 5. 当前状态
- 是否完成：✅
