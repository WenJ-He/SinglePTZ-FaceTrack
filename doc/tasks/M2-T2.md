### 任务完成报告

#### 1. 任务信息
- 任务ID：M2-T2
- 任务名称：ORT provider自动选择

#### 2. 实现内容
- 在 `src/config.py` 中新增 `auto_providers()` 函数
- 检测 CUDAExecutionProvider 可用性，不可用时回退 CPU 并打 INFO 日志

#### 3. 修改/创建文件
- `src/config.py`（修改）

#### 4. 测试情况
- 测试方式：调用 auto_providers(prefer_gpu=True)
- 测试结果：无GPU环境下正确返回 CPUExecutionProvider

#### 5. 当前状态
- 是否完成：✅
