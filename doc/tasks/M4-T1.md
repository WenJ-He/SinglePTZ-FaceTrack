### 任务完成报告

#### 1. 任务信息
- 任务ID：M4-T1、M4-T2
- 任务名称：状态机骨架 + PATROL_GOTO/PATROL_DWELL 实现

#### 2. 实现内容
- 实现 `src/scheduler/state_machine.py`：ScanScheduler 类
- State(IntEnum) 12个状态枚举
- PATROL_GOTO：调用 goto_preset + 设 settle 超时
- PATROL_DWELL：稳图等待 + 持续1280检测 + min_confirm_frames 连续确认 + 停留超时切下一个预设位
- SCAN 状态占位桩（M5 完善）
- 帧差稳图判定 `_frame_settled`
- 扫描顺序构建 `_build_scan_order`
- 命令处理：q退出/p暂停/h回家/r重置

#### 3. 修改/创建文件
- `src/scheduler/state_machine.py`（新建）

#### 4. 测试情况
- 测试方式：语法验证 + 逻辑审查
- 测试结果：语法正确，状态流转符合设计书§5.10

#### 5. 当前状态
- 是否完成：✅
