### 任务完成报告

#### 1. 任务信息
- 任务ID：M4-T3、M4-T4
- 任务名称：主程序入口 + 巡航联调

#### 2. 实现内容
- 实现 `src/main.py`：完整启动序列
- 启动流程：加载config → 设LD_LIBRARY_PATH → SDK login → 3D定位检查 → ORT sessions → ArcFace + Gallery → RTSP → Scheduler
- 优雅退出：SIGINT/SIGTERM 信号处理
- Stdin 读取线程，支持终端命令输入
- 3D定位检查改为非阻塞（失败时警告继续运行）

#### 3. 修改/创建文件
- `src/main.py`（新建）

#### 4. 测试情况
- 测试方式：语法验证 + 实际运行
- 测试结果：SDK登录成功，能力集查询已容错

#### 5. 当前状态
- 是否完成：✅
