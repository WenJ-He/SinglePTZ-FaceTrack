### Task Report

#### 1. Task Info
- Task ID: M5-T1 ~ M5-T6
- Task Name: Single preset scan

#### 2. Implementation
- Full SCAN state flow in state_machine.py:
  - SCAN_GOTO_PRESET, SCAN_DETECT, SCAN_PICK, SCAN_SETTLE
  - SCAN_CAPTURE with CaptureTracker integration (B+C strategy)
  - SCAN_RECOGNIZE with ArcFace + Gallery matching
  - SCAN_ZOOM_OUT, SCAN_NEXT_PRESET
- `src/scheduler/capture_tracker.py`: CaptureTracker with safe zone, danger zone, 3D correction, Kalman filter
- Event logging (events.jsonl) and stranger snapshot saving
- main.py updated to pass arcface and gallery to scheduler

#### 3. Test
- CaptureTracker unit tests: all 4 tests passed (giveup, collect, settle, safe zone boundary)
- Syntax verified for all files

#### 4. Status
- Complete: Yes
