### Task Report

#### 1. Task Info
- Task ID: M6-T1 ~ M6-T5
- Task Name: Multi-preset + ReID

#### 2. Implementation
- `src/reid/osnet.py`: OSNetReID embedder (512-d, L2 normalized)
- `src/track/sort_reid.py`: SORT+ReID tracker with Hungarian matching (IoU+ReID cost), EMA update, snapshot/restore
- OSNet_x0_25 ONNX model exported to models/reid/
- Global identified table with cross-preset dedup in state machine
- Tracker snapshot/restore on preset transitions
- SCAN_DETECT uses tracker for identity maintenance and cross-preset dedup

#### 3. Test
- OSNetReID: 512-d output, L2 norm=1.0, self-similarity=1.0
- All syntax verified

#### 4. Status
- Complete: Yes
