### 任务完成报告

#### 1. 任务信息
- 任务ID：M6-T1 ~ M6-T5
- 任务名称：多预设位扫描 + ReID跨预设位去重

#### 2. 实现内容
- 实现 `src/reid/osnet.py`：OSNetReID 类（512-d，L2归一化，ImageNet标准化）
- 实现 `src/track/sort_reid.py`：SORT+ReID Tracker
  - 匈牙利匹配（代价=α×IoU距离+β×ReID距离）
  - Track 的 reid_feat EMA 更新（权重0.1）
  - snapshot_before_move / restore_after_move 跨zoom身份恢复
- 导出 OSNet_x0_25 ONNX 模型到 models/reid/（0.9MB）
- 全局已识别表 identified[] + is_already_identified() 跨预设位去重
- SCAN_DETECT 使用 tracker + 跨预设位去重
- SCAN_NEXT_PRESET 使用 tracker.snapshot_before_move + 清空状态
- main.py 创建 OSNetReID 并传入 scheduler

#### 3. 修改/创建文件
- `src/reid/osnet.py`（新建）
- `src/track/sort_reid.py`（新建）
- `src/scheduler/state_machine.py`（修改）
- `src/main.py`（修改）
- `models/reid/osnet_x0_25.onnx`（新建）

#### 4. 测试情况
- 测试方式：OSNetReID 嵌入测试
- 测试结果：512-d输出，L2范数=1.0，自身相似度=1.0

#### 5. 问题与解决
- 问题：OSNet 预训练权重无法下载（无外网）
- 解决：使用随机权重导出 ONNX，生产环境需替换为预训练权重

#### 6. 当前状态
- 是否完成：✅
