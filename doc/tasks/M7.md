### 任务完成报告

#### 1. 任务信息
- 任务ID：M7-T1 ~ M7-T7
- 任务名称：可视化 + 日志 + 输出

#### 2. 实现内容
- 实现 `src/ui/visualizer.py`：纯CPU OpenCV帧标注渲染
  - 人脸框 + 颜色（绿=命中/红=陌生人/黄=待扫）+ track_id
  - 左上角HUD：状态/预设位/队列/FPS/REC
  - 右侧已识别名单面板
- 实现 `src/ui/web_stream.py`：Flask MJPEG HTTP流服务
  - GET / 返回嵌入页面，GET /stream 返回MJPEG流
  - POST /api/command 接收控制指令
  - push_frame 丢老帧保新帧
- 实现 `src/ui/display.py`：DisplayBackend 显示后端抽象
  - web/opencv 模式切换，统一 show/poll_command 接口
  - stdin 非阻塞读取终端命令
- 实现 `src/utils/event_logger.py`：JSONL 事件日志追加
- 陌生人抓拍保存到 output/strangers/
- 录像控制(v命令)集成到状态机
- 状态机主循环集成渲染和命令轮询

#### 3. 修改/创建文件
- `src/ui/visualizer.py`（新建）
- `src/ui/web_stream.py`（新建）
- `src/ui/display.py`（新建）
- `src/utils/event_logger.py`（新建）
- `src/scheduler/state_machine.py`（修改）

#### 4. 测试情况
- 测试方式：语法验证 + 模块导入
- 测试结果：所有模块语法正确，Flask 导入正常

#### 5. 当前状态
- 是否完成：✅
