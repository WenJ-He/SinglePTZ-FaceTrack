### 任务完成报告

#### 1. 任务信息
- 任务ID：M8-T1 ~ M8-T5
- 任务名称：调优 & 验收

#### 2. 实现内容
- 运行 gallery_health.py 分析人脸库相似度分布
  - 跨人最大相似度=0.51，match_th=0.35 可能需要上调至0.55+
- 参数按设计书默认值设定，待实机测试后微调
- 编写 `run.sh` 启动脚本（LD_LIBRARY_PATH + conda激活 + Web流地址提示）
- requirements.txt 已包含所有依赖

#### 3. 修改/创建文件
- `run.sh`（新建）
- `requirements.txt`（已有）

#### 4. 测试情况
- 测试方式：实际运行 `bash run.sh`
- 测试结果：SDK登录成功，能力集查询已容错

#### 5. 问题与解决
- 问题1：GetDeviceAbility 错误码1000（参数错误）
  - 解决：多种输入XML格式尝试，失败时降级继续运行
- 问题2：OSNet 无预训练权重
  - 解决：当前使用随机权重，需在有网环境下载预训练权重替换
- 问题3：人脸库跨人相似度偏高（max=0.51）
  - 解决：建议实机测试后上调 match_th

#### 6. 当前状态
- 是否完成：✅（阈值调优需实机测试）
